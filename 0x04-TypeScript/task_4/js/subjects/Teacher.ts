namespace Subjects {
  // Defining the Teacher interface with first name and last name properties
  export interface Teacher {
    firstName: string;  // Teacher's first name (required)
    lastName: string;   // Teacher's last name (required)
  }
}
